// This file has been disabled to prevent deployment issues
// No longer needed as contact form uses mailto
export function set() {
  return Promise.resolve();
}

export function get() {
  return Promise.resolve(null);
}

export function getByPrefix() {
  return Promise.resolve([]);
}